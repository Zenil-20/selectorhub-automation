{
  "name": "selectorhub-automation",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Chrome extension + Node backend for generating stable Playwright/Cypress/Selenium locators.",
  "scripts": {
    "test": "node --test tests/*.test.js"
  },
  "devDependencies": {
    "jsdom": "^24.0.0"
  }
}
